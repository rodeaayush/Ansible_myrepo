--- 
- name: My Global Variable playbook
  hosts: all
  vars:
    URL: "www.office.com"  # Corrected line: Added quotes around the URL value

  tasks:
    - name: My Task one for global variable playbook
      debug:
        msg: "{{ URL }}"
...
